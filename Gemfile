source "https://rubygems.org"

gem "isomer-jekyll", group: :jekyll_plugins

# Jekyll 4.2 (pinned by isomer-jekyll) needs webrick for `jekyll serve`.
# Ruby 3.0+ dropped webrick from stdlib, so it must be declared explicitly.
gem "webrick"
